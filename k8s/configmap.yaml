apiVersion: v1
kind: ConfigMap
metadata:
  name: scheduler-api-config
  namespace: platform
data:
  SPRING_PROFILES_ACTIVE: docker
  POSTGRES_HOST: postgres
  POSTGRES_PORT: "5432"
  POSTGRES_USER: scheduler
  POSTGRES_PASSWORD: scheduler
  POSTGRES_DB: scheduler
